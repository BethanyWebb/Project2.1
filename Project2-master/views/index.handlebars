<div class="container">
	<div class="row">
		<div class="col-md-12 text-center" id="top-bar">
			<img src="/assets/img/burger.png" alt="burger" />
			<h1> Eat-Da-Burger! </h1>
		</div>

		<div class="form-inline col-md-4 text-center" class="task">
			<div class="row">
			{{#each universities}}
			<h1>{{this.first_name}}</h1>
			<h1>{{this.last_name}}</h1>
			<h1>{{this.email_address}}</h1>
			<h1>{{this.password}}</h1>

			<h1>{{this.nationality}}</h1>
			<h1>{{this.desired_country}}</h1>
			<h1>{{this.highest_education}}</h1>
			<h1>{{this.student_visa}}</h1>
			{{/each}}
			</div>
		</div>
	</div>
</div>
